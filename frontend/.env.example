# Vercel Configuration

# Development (Local)
# NEXT_PUBLIC_API_URL=http://localhost:3911
# NEXT_PUBLIC_VERCEL_URL=http://localhost:3000
# NEXT_PUBLIC_APP_ENV=development

# Production (Vercel - set these in Vercel dashboard)
# NEXT_PUBLIC_API_URL=https://dev-content-multiplier-01-backend.vercel.app
# NEXT_PUBLIC_APP_ENV=production
# NODE_ENV=production
# VERCEL_ENV=production

# Important:
# - MUST use HTTPS for production (prevents Mixed Content errors)
# - NEXT_PUBLIC_API_URL should point to backend domain
# - Backend domain: dev-content-multiplier-01-backend.vercel.app
# - Frontend will append /api automatically
# - VERCEL_URL should NOT be set (or delete it if in dashboard)
